//const body = document.body
//const div = document.querySelector('div')
//const spanHi = document.querySelector('#hi')
//const spanBye = document.querySelector('#bye')
//spanBye.remove()  // remove the span
//div.append(spanBye) // put the span back
//div.removeChild(spanHi) // another way to remove but not recommended
//console.log(spanHi.getAttribute("id")) // this is the method to get the value of
//// the parameter we put in parentheses
//console.log(spanHi.title) // this is the method to get the value of the attribute
//// but more simple way
//console.log(spanHi.setAttribute("id", "blalbla")) // this is to change the id to
//// blalbla
//spanHi.id = "bopbop" // this is to change the id to bopbop in a more simple way
//spanHi.removeAttribute("id") // this is to remove the id
// the dataset method contain all the data
// to access a specific data inside it you have to do it like below
//console.log(spanHi.dataset.text)
//spanHi.dataset.newName = "hi" // this is to set a new data in the span
//spanHi.classList.add('new-class') // this is to add another class in the span
//spanHi.classList.remove('new-class') // this is to remove the class in the span
//spanHi.classList.toggle('hi2', true) // this gonna check if hi2 exist and dont touch
//// it if it exist already or add it if not
//spanHi.classList.toggle('hi2', false) // this gonna check if hi2 exist and remove
//// it if it exist already

//spanHi.style.color = "black" // this is like the css but in javascript way to modify
//// the style
//spanHi.style.backgroundColor = "red" // this is like the css but in javascript way to modify
//// the style

//const div = document.createElement("div")
//div.innerText = "Hello World"  // this one is overridden by the second
//div.textContent = "Another way to add text"
//div.innerHTML = "<strong> Another way to add text </strong>" // we need the
// innerHTML to put balises

// much secure way to create balises with content
//const strong = document.createElement("strong")
//strong.innerText = " bob "
//div.append(strong)
//
//body.append(div)

//const div = document.querySelector('div')
//
//console.log(div.textContent) // textContent gonna print everything inside the div
//// with the exact indentation
//console.log(div.innerText) // innerText gonna only print what we see on the screen

